language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI server Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# CI alarm upon execution-completion
notifications:
  email:
    recipients:
      - 83tachyon@gmail.com
      -